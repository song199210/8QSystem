import {createStore} from "redux";

const state={//state数据源

};

function reducer(state,action){//
    switch(action.type){
        case "ADD":

            break;
        case "DELETE":
    }
}
export const Store=createStore(reducer);
